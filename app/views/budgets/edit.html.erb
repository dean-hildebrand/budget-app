<% content_for :title, "Editing budget" %>

<%= link_to "← Back to budget", @budget, class: "back-link" %>

<div class="page-header">
  <div>
    <span class="page-header__eyebrow"><%= @budget.formatted_month %></span>
    <h1>Edit budget</h1>
  </div>

  <div class="page-header__actions">
    <%= link_to "All budgets", budgets_path, class: "btn btn--secondary" %>
  </div>
</div>

<%= render "form", budget: @budget %>
