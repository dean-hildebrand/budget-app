<% content_for :title, "New budget" %>

<%= link_to "← Back to budgets", budgets_path, class: "back-link" %>

<div class="page-header">
  <div>
    <span class="page-header__eyebrow">Budgets</span>
    <h1>New budget</h1>
  </div>
</div>

<%= render "form", budget: @budget %>
