<%# Not routed: config/routes.rb nests transactions and excludes :index. %>
<% content_for :title, "Transactions" %>

<div class="page-header">
  <h1>Transactions</h1>
</div>

<div id="transactions" class="card-grid">
  <% @transactions.each do |transaction| %>
    <%= render transaction %>
  <% end %>
</div>
